import { SideImage } from "."
import { Form } from "."

const Login = () => {
  return (
    <main className="flex text-center items-center">
      <SideImage />
      <Form/>
    </main>
  )
}

export default Login
